export default function InstagramWordmark({ className = "" }) {
  return (
    <div className={`text-5xl leading-none text-igText font-insta select-none ${className}`}>
      Instagram
    </div>
  );
}
